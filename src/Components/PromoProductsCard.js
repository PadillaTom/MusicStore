import React from 'react';

const PromoProductsCard = ({
  id,
  imageUrl,
  price,
  promoted,
  title,
  subtitle,
}) => {
  return <article className='promoted-card'>Hello fom promo</article>;
};

export default PromoProductsCard;
