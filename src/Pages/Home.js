import React from 'react';
// Components:
import Banner from '../Components/Banner';

const Home = () => {
  return (
    <section className='section'>
      <Banner></Banner>
    </section>
  );
};

export default Home;
